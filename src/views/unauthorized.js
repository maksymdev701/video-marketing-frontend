const UnauthorizePage = () => {
  return (
    <div
      style={{
        height: "15rem",
        marginTop: "2rem",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1 style={{ color: "#1f1e1e", fontWeight: 500 }}>Unauthroized Page</h1>
    </div>
  );
};

export default UnauthorizePage;
